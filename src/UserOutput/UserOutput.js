import React, { Component } from 'react';


const userOutput = (props) => {
    return (
        <div>   
            <p>{props.username}</p>
            <p>Hi!</p>
        </div>
    );
}

export default userOutput;